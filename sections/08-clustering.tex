\section{Clustering}             % grouping the data using the features above

\hint{Describe the clustering method, its inputs and parameters (e.g.\ number of
clusters), how the result was evaluated, and what the clusters show.}
